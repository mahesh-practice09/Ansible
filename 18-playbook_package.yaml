---
- name: Install packages
  hosts: local
  connection: local
  become: true
  tasks:
   - name: Install packages 
     ansible.builtin.package:
       name:  "{{ item }}"
       state: present
     loop:
       - mysql
       - nginx
       - zip