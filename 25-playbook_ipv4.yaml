---
- name: check if it is ipv4
  hosts: local
  connection: local
  vars:
    ipv4: 192.33.22.344
  tasks:
  - name: check if it is ipv4
    ansible.builtin.debug:
      msg: "{{ ipv4 | ansible.builtin.ipv4 }}"
