---
- name: convert dict to items
  hosts: local
  connection: local
  tasks:
  - name: get file students.yaml
    ansible.builtin.set_fact:
      students_data: "{{ lookup('file',students.yaml) | from_yaml }}"

  - name:
    ansible.builtin.debug:
      msg: "{{ students_data | to_nice_json }}"
